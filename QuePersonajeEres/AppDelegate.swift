//
//  AppDelegate.swift
//  QuePersonajeEres
//

import UIKit

@main
class AppDelegate: UIResponder, UIApplicationDelegate {

    var window: UIWindow?

    func application(_ application: UIApplication,
                      didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
        // El storyboard principal (Main) se carga automáticamente porque
        // está definido como "UIMainStoryboardFile" en Info.plist.
        return true
    }
}
